/*
 * This is a custom ESLint configuration for use with
 * internal libraries that utilize storybook.
 *
 * This config extends the eslint-plugin-storybook for projects using storybook.
 * For more information, see https://github.com/storybookjs/eslint-plugin-storybook
 */

module.exports = {
  extends: ['plugin:storybook/recommended'],
};
